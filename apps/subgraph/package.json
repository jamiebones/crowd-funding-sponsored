{
    "name": "@blockchain-monorepo/subgraph",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ crowd-funding",
        "create-local": "graph create --node http://localhost:8020/ crowd-funding",
        "remove-local": "graph remove --node http://localhost:8020/ crowd-funding",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 crowd-funding",
        "clean": "rm -rf build generated"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "^0.68.0",
        "@graphprotocol/graph-ts": "^0.32.0"
    }
}